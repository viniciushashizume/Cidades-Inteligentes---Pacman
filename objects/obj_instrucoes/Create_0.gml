// Parâmetros de botão
btn_largura = 200;
btn_altura = 60;
btn_comecar_x = (room_width - btn_largura) / 2;
btn_comecar_y = room_height - 100;

// Variáveis para animação
texto_offset = -100;  // Inicia fora da tela
texto_alvo = 0;  // A posição final do texto

// Variáveis para o efeito de hover
mouse_sobre_btn = false;
